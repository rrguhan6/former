Kind of google form clone

Installation:

    client:
        0.goto client directory
        1.npm install
        2.npm run start

    server:
        0.goto server directory
        1.pip3 install -r requirements.txt
        2.python3  run.py


    Goto localhost:3000


using docker :
    1. change proxy as http://server:4000/"
    2. docker-compose up